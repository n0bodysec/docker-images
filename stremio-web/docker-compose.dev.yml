version: '3.9'

services:
  stremio:
    image: stremio
    build: .
    ports:
      - '11470:11470'
      - '8080:8080'
    volumes:
      - ./data/:/root/.stremio-server
    environment:
      APPLY_PATCHES: 1
      FIX_UNSUPPORTED_MEDIA: 1
      NO_CORS: 1
